{
  "name": "prmonitor",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "rm -rf build; react-scripts build && cp src/background.js build/background.js",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}